export interface Partner {
  latitude: string;
  longitude: string;
  partner_id: number;
  name: string;
}

export interface eligibleParnter {
  partner_id: number;
  name: string;
  distance: number;
}
